# Security

Do not open public issues for credentials, token leaks, vulnerabilities, or
private SDK/customer data. Report security-sensitive concerns through the
private maintainer channel used for the affected project.

Do not commit `.env` files, PyPI tokens, private corpora, customer data, or
downloaded proprietary SDK bundles. Use `.env.example` only for non-secret
variable names and placeholders.
